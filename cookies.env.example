# Copy this file to cookies.env and paste your browser cookie string below.
# Get it from: DevTools → Network → any author.today request → Request Headers → Cookie
# Refresh whenever you get 403s or empty results (session cookies expire).
#
# IMPORTANT: never commit cookies.env to git — it contains your login session.

COOKIES=__ddg8_=REPLACE_ME; LoginCookie=REPLACE_ME; TrustedToken=REPLACE_ME
